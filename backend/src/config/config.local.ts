import { parseEnv } from './util'

export default {
  app: {
    rpcUrl: 'https://testnet-rpc.thundercore.com',
    thunderScanUrl: 'https://scan-testnet.thundercore.com',
    mnemonic:
      'sentence heavy print cereal steel near reveal fix tuition need consider speak range provide six',
    jwtSecret: 'my_secret_sss',
    keySecret: 'my_key_secret_sss'
  },
  debug: {
    benchmark: true
  }
}
